// initally player is a silouette, game logic checks if the guess is correct after every guess. If it is correct, then render the player image by calling api

const Player = () => {
    return(
        <>
            <div>
                Player will go here
            </div>
        </>
    )
}

export default Player
